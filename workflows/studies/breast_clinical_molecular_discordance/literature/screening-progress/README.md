# Screening Progress Receipts

Place sequential, verified aggregate receipts here as `batch-0001.yaml`,
`batch-0002.yaml`, and so on. Never overwrite or renumber a receipt. Each receipt
binds the cumulative immutable decision ledger to the preceding `progress_id`.

No titles, abstracts, full text, API exports, credentials, or model output belong
in this directory.
