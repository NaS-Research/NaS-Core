# AI Advisory Receipts

Store sequential Git-safe aggregate receipts for AI advisory runs here. Requests,
abstracts, sentence text, model output, and provider responses remain in external
object storage. Receipts never represent human decisions or scientific evidence.
