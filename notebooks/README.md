# Notebooks

Notebooks are exploratory. Any analysis used in a verified claim or publication
must be moved into a versioned, deterministic workflow with captured inputs,
parameters, environment, and outputs.
